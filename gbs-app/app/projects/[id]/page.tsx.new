// Fix for the template literal issue - line 844-848
// Original (broken):
// <span className={`font-bold ${
//   balance >= 0 ? 'text-green-400' : 'text-red-400'
// }`}>
//   {formatCurrency(balance)}

// Fixed:
<span className={`font-bold ${
  balance >= 0 ? 'text-green-400' : 'text-red-400'
}`}>
  {formatCurrency(balance)}
